# Python dependencies used by `rake benchmark:deps` / benchmark comparisons.
mlx
# Graph IR -> ONNX conversion support.
onnx
# Optional local runtime validation.
onnxruntime
# Safetensors fallback conversion used by web asset exporters.
numpy
safetensors

# Documentation build dependencies.
sphinx
breathe
sphinx-book-theme
sphinx-copybutton
